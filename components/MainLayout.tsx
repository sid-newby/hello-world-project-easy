import AppLayout from "./AppLayout"; // Import the new AppLayout

const MainLayout = () => {
  // Use the AppLayout component which contains the Outlet
  return <AppLayout />;
};

export default MainLayout;
